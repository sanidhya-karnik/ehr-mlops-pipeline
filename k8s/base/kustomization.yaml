apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mlops

labels:
  - pairs:
      app.kubernetes.io/part-of: mimic-mlops
      app.kubernetes.io/managed-by: kustomize
    includeSelectors: false

resources:
  - namespace.yaml
  - model-serving.yaml
  - jobs.yaml
